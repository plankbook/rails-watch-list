<div class='container'>
  <h1>Movie lists</h1>
    <div>
      <p><% @lists.each do |list| %>
        <li><%= link_to list.name, list_path(list) %></li>
        <hr>
        <% end %>
      </p>
    </div>
</div>
